# CRYPTO PUZZLE - TOKENS DISCOVERED

This file documents each token found so far and the concrete cryptographic proof that it is correct.

## Token 1: nsgjr
- **Size**: 5 characters
- **Source**: Decrypting `details_file.txt`
- **Proof**: When we apply a Caesar Shift of 20 (on a 27-character alphabet: space + lowercase letters) to the ciphertext in `details_file.txt`, it perfectly decrypts into readable English sentences. The very last sentence explicitly states: "submit the first solution to blackboard which is nsgjr". 
- **Confidence**: 100%

## Token 2 (Passphrase): 12345667
- **Size**: 8 characters
- **Source**: Cracking `hash.txt`
- **Proof**: The decrypted instructions from Step 1 specifically state: 
  1. "the passphrase is your id concatenated with two magic digits" (ID is 123456)
  2. "the passphrase is stored hashed in your files" 
  3. "sha two five six" (SHA-256)
  Our Python script tested every 2-digit combination attached to "123456". It found that the string "12345667\n" (ID + the digits 67 + a newline) produces the exact SHA-256 hash found in `hash.txt`:
  Hash: aaa6b6e8d9d1448b1d1b8d32de7e67a5e965a1010bccc43485eef1475b958d8c
  It is practically impossible to find a collision for a SHA-256 hash by accident. Therefore, this must be the exact passphrase generated by the instructor.
- **Confidence**: 100%

## Token 3 (Symmetric Key): 4072023370
- **Size**: 10 characters
- **Source**: Decrypting `encryptedKeyAES.txt` using `private.pem`
- **Proof**: 
  1. The instructions from Step 1 say: "reveal the magic passphrase and use it to decrypt the symmetric key"
  2. The symmetric key is stored in `encryptedKeyAES.txt`.
  3. The file `private.pem` is an RSA private key that is password-protected. 
  4. When we pass our cracked passphrase (`12345667`) to OpenSSL to unlock the `private.pem` file, OpenSSL successfully accepts the password and unlocks the RSA key. If the password were wrong, OpenSSL would immediately reject it with a "bad decrypt / bad password read" error.
  5. By using this unlocked RSA key to decrypt `encryptedKeyAES.txt`, we successfully retrieve the string "4072023370". If we used the wrong key to decrypt it, RSA decryption would fail mathematically with a padding error. Instead, it extracts perfectly clean plaintext.
- **Confidence**: 100%

## Token 4: [NOT FOUND YET]
- **Size**: 104 characters
- **Source**: Expected to be found by decrypting `problem_file.enc` (AES encrypted) using the symmetric key (4072023370).
- **Current Status**: All standard OpenSSL brute-force attempts with AES/DES ciphers using the key 4072023370 have failed to decrypt the file into readable text.
